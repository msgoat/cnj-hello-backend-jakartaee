# docker-compose.yml
# ----------------------------------------------------------------------------
# Composes Docker containers and volumes to a runnable Java EE application
# ----------------------------------------------------------------------------
#
---
services:

  backend:
    image: docker.cloudtrain.aws.msgoat.eu/cloudtrain/cnj-hello-backend-jakartaee:latest
    ports:
      - "38080:8080"

